<div class="container">
  <% @books.each do |book| %>
<div class="row">
  <div class="col-lg-3  col-md-3 ">
    <%= link_to (image_tag(book.image, class:"card-img-top")), book %>
      <h5><%= book.title %></h5>

     <h5>Par <%= book.author %></h5>

      <h5> Ajouté par<%= image_tag(book.user.avatar, class: "img-circle avatar-small") %> <%= book.user.full_name %></h5>


 </div>
 <% end %>
</div>

    <%= link_to "S'inscrire",  new_user_registration_path %>
    <%= link_to "Connexion",  new_user_session_path %>
    <%= link_to "Déconnexion",  destroy_user_session_path, method: :delete %>
</div>

<div class="card">
                    <img class="card-img-top" src="http://success-at-work.com/wp-content/uploads/2015/04/free-stock-photos.gif">
                    <div class="card-block">
                        <h5 class="text-bold">Tawshif Ahsan Khan</h5>
                    </div>
                </div>
